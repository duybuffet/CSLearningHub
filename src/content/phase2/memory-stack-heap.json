{
  "id": "phase2_memory-stack-heap",
  "title": "Memory: Stack & Heap",
  "phase": 2,
  "week": 4,
  "icon": "memory",
  "tagline": "Where your variables actually live — and why confusing value with reference silently breaks code.",
  "readTime": "~22 min",
  "sections": [
    {
      "type": "intro",
      "content": "Every time you declare a variable or create an object, the runtime has to find physical space for it somewhere in RAM. That *somewhere* is not random — virtually every modern language runtime splits memory into (at least) two fundamentally different regions: the **stack** and the **heap**. Each region has its own allocation strategy, its own lifetime rules, and its own performance profile. The choice of which region holds a given value shapes nearly every aspect of how your code behaves.\n\nUnderstanding the stack and heap is one of those rare topics where a single idea unlocks a dozen other things simultaneously. It explains why copying an integer is always safe while copying an object can surprise you. It explains why functions clean up after themselves automatically but forgetting to release heap memory causes memory leaks. It explains why `const` does not make an object immutable, why closures can outlive the function that created them, why recursion can crash with a stack overflow, and why garbage collectors exist at all.\n\nThis is also a perennial favourite in technical interviews — and for good reason. Engineers who understand this topic write fewer bugs, debug faster, and make better architectural decisions. The concept is simple in principle, but its implications run deep. In this chapter we will build the full picture: what each region is, how allocation and deallocation work, how JavaScript maps its types onto these regions, the mechanics of garbage collection, call-stack frames, closures and their relationship to the heap, common pitfalls, and real-world performance implications.\n\nWe will use JavaScript as our primary lens — it is the most common first language in web development — but the core ideas apply equally to Python, Java, Go, Rust, and C. Where those languages differ meaningfully, we will note why."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart TD\n  A[\"Process Memory Space\"] --> B[\"Stack Region\\n(small · fast · LIFO)\"] \n  A --> C[\"Heap Region\\n(large · flexible · GC-managed)\"]\n  B --> D[\"Function frames\\n(params · locals · return addr)\"]\n  B --> E[\"Primitive values\\n(number · boolean · etc.)\"]\n  C --> F[\"Objects & arrays\\n(reference types)\"]\n  C --> G[\"Closures & upvalues\\n(captured variables)\"]\n  G --> H[\"Garbage Collector\\nreclaims unreachable nodes\"]",
      "caption": "A process's memory is partitioned: the stack holds function frames and primitive values with automatic, deterministic cleanup; the heap holds objects and closures whose lifetime is managed by the garbage collector."
    },
    {
      "type": "analogy",
      "title": "A Restaurant Kitchen and Its Walk-In Refrigerator",
      "content": "Picture a busy restaurant kitchen. Each chef (function) is assigned a numbered prep station when they start working. The station is small and neatly organised — it holds exactly what that chef needs right now: a recipe card (the return address telling the chef where to go when done), a few pre-measured bowls of ingredients (parameters and local primitives), and space for the dish taking shape. When the chef finishes, the station is wiped clean instantly and reassigned to the next cook. No one has to individually wash each bowl — the whole surface is cleared in one motion by moving a single pointer. That is the **stack**.\n\nNow picture the walk-in refrigerator at the back of the kitchen. It is enormous. Ingredients that do not fit at any prep station — a whole leg of lamb, a 20-litre vat of stock — live here. A chef keeps a ticket slip with the fridge location of their ingredient. Multiple chefs can hold tickets pointing to the *same* item; if one chef tastes the stock and adds salt, every other chef using that same vat will find it saltier. That shared mutable access is what happens when two variables reference the same object. The kitchen manager periodically surveys the refrigerator and throws out any ingredient that no chef has a ticket for any more — that is **garbage collection**.\n\nThe key asymmetry: the prep station (stack frame) is tied to one chef's shift and vanishes when the shift ends. The walk-in fridge (heap) exists independently; items stay there until *no one* needs them."
    },
    {
      "type": "explanation",
      "title": "The Stack: Deterministic, Automatic, and Fast",
      "content": "The **call stack** is a contiguous block of memory managed with a single moving pointer called the **stack pointer**. When a function is called, the runtime **pushes** a new **stack frame** onto the stack — it moves the pointer to claim a block of memory large enough for:\n\n- The function's **parameters** (copies of the arguments passed in)\n- **Local variables** declared inside the function\n- The **return address** — the instruction pointer value to jump back to when the function returns\n- On some architectures, the **saved frame pointer** for the calling function\n\nWhen the function returns, the runtime **pops** the frame by simply moving the stack pointer back to where it was. The memory is not zeroed or individually freed; it is just abandoned in place and will be overwritten by the next push. This makes stack allocation essentially free — it is a single pointer arithmetic operation, with no searching for free space, no bookkeeping, and no per-variable cleanup.\n\nThe price of this simplicity is that the stack is **strictly LIFO**: you can only reclaim the most recently pushed frame. You cannot \"free\" a frame in the middle of the stack without first unwinding everything above it. This is why local variables have exactly the same lifetime as the function call that created them — no more, no less.\n\nA typical thread's stack is 1–8 MB (configurable per OS and language runtime). That sounds large, but each recursive call pushes a new frame. A function that calls itself a million times — or even ten thousand times on a large-frame function — will exhaust the stack and trigger a **stack overflow** error: `RangeError: Maximum call stack size exceeded` in JavaScript, `RecursionError` in Python, a segmentation fault in C."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart TD\n  SP[\"Stack Pointer →\"] --> F3[\"greet() frame\\nname = 'Alice'\\n[return addr: main+12]\"] \n  F3 --> F2[\"processUser() frame\\nid = 42\\n[return addr: main+8]\"]\n  F2 --> F1[\"main() frame\\nresult = ref-A\\n[return addr: OS entry]\"]",
      "caption": "The call stack at the moment greet() is executing. Frames are stacked LIFO. When greet() returns its frame is popped, the stack pointer moves down, and processUser() resumes. Primitives like id=42 and name='Alice' live directly inside their frames."
    },
    {
      "type": "explanation",
      "title": "The Heap: Flexible, Long-Lived, and GC-Managed",
      "content": "The **heap** is a large, unstructured pool of memory — typically many hundreds of megabytes or more, limited primarily by available physical RAM and OS settings. Unlike the stack, there is no ordering constraint. You can allocate a block at any time, and it will persist until explicitly freed (in C/C++/Rust) or until the garbage collector determines it is unreachable (in Java, JavaScript, Python, Go).\n\nHeap allocation is more expensive than stack allocation because the runtime must:\n1. **Find a free block** large enough for the requested size — this requires searching a free-list or using a slab allocator\n2. **Mark the block as used** so it will not be handed out again\n3. Return a **pointer** (memory address) to the caller\n\nIn JavaScript you never call `malloc()` directly — the engine handles all of this automatically whenever you write `{}`, `[]`, or `new SomeClass()`. But the overhead is real, and it is one reason that creating many tiny short-lived objects in a tight loop can stress the garbage collector.\n\nHeap objects are accessed **indirectly via references**. A variable on the stack does not hold the object itself — it holds the memory address of the heap location where the object lives. Two variables can hold the same address, meaning they both point to the same chunk of heap memory. Any mutation through one pointer is immediately visible through the other, because they are both looking at the same physical bytes.\n\nWhen no live variable holds a reference to a heap object — when it becomes **unreachable** — the memory it occupies is wasted until the garbage collector finds and reclaims it. The lag between an object becoming unreachable and its memory being freed is what makes heap lifetime *indefinite* rather than deterministic."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart LR\n  SV1[\"Stack variable\\nuser1 (ref → 0x4A20)\"] --> H1[\"Heap object @ 0x4A20\\n{ name: 'Alice'\\n  score: 42 }\"]\n  SV2[\"Stack variable\\nuser2 (ref → 0x4A20)\"] --> H1\n  H2[\"Heap object @ 0x5B30\\n{ city: 'Tokyo' }\"]\n  SV3[\"Stack variable\\norder (ref → 0x5B30)\"] --> H2",
      "caption": "Both user1 and user2 hold the same heap address. Mutating through user2 changes what user1 sees — there is only one object. order points to a separate heap object at a different address."
    },
    {
      "type": "explanation",
      "title": "Primitives vs. Reference Types in JavaScript",
      "content": "JavaScript divides its value types into two categories with very different memory semantics.\n\n**Primitives** — `number`, `string`, `boolean`, `null`, `undefined`, `symbol`, and `bigint` — are stored and passed **by value**. When you assign a primitive to a new variable, the runtime copies the raw bits of the value into a fresh stack slot. The original and the copy are completely independent; modifying one has zero effect on the other. The copy does not point back to the original in any way.\n\nA subtle clarification: in V8 (Node.js / Chrome) and other modern JavaScript engines, small integers are often stored as **immediate values** inside pointers (tagged integers, or \"Smis\") rather than on the stack in the traditional sense — this is an implementation detail. The observable behaviour is the same as value semantics: assignment copies, and the copy is independent.\n\n**Objects, arrays, and functions** — everything that is not a primitive — are **reference types**. The value that sits in a variable is not the object itself but a **reference**: a numeric memory address pointing to the object on the heap. When you assign a reference-type variable to another variable, only the reference (the address) is copied, not the heap data it points to. Both variables end up referencing the same object.\n\nThis distinction has huge practical consequences:\n- Passing a primitive to a function is always safe from the caller's perspective — the function gets its own copy.\n- Passing an object to a function gives the function access to the same heap data. A function that mutates `obj.count` modifies the original object seen by the caller.\n- Comparison with `===` for objects tests reference equality (\"are these the same object in memory?\"), not structural equality (\"do these objects have the same contents?\"). Two objects with identical properties return `false` from `===` if they occupy different heap addresses."
    },
    {
      "type": "code_example",
      "language": "javascript",
      "label": "Example",
      "code": "// ── Primitives: assignment copies the VALUE ───────────────────\nlet a = 10;\nlet b = a;    // b gets an independent copy of the number 10\nb = 99;\nconsole.log(a); // 10 — completely unaffected\n\n// ── Objects: assignment copies the REFERENCE ──────────────────\nconst user1 = { name: 'Alice', score: 42 };\nconst user2 = user1;   // user2 holds the SAME heap address as user1\nuser2.score = 100;\nconsole.log(user1.score); // 100 — same heap object!\n\n// ── Reference equality vs. structural equality ────────────────\nconst objA = { x: 1 };\nconst objB = { x: 1 };\nconst objC = objA;\nconsole.log(objA === objB); // false — different heap addresses\nconsole.log(objA === objC); // true  — same heap address\n\n// ── Passing objects to functions ──────────────────────────────\nfunction addBonus(player) {\n  player.score += 50;  // mutates the caller's object!\n}\nconst p = { name: 'Bob', score: 200 };\naddBonus(p);\nconsole.log(p.score); // 250 — caller sees the mutation\n\n// ── Strings look mutable but are immutable primitives ─────────\nlet s1 = 'hello';\nlet s2 = s1;\ns2 = s2.toUpperCase(); // creates a NEW string; does not mutate s1\nconsole.log(s1); // 'hello' — unchanged",
      "explanation": "The first block shows that primitives copy their value. The second shows that objects share a reference — user2 is an alias for user1, not a copy. Reference equality (===) tests the address, not content. The function example shows the real-world danger: addBonus() modifies the caller's data because it received a reference, not a copy. Strings are heap-allocated in V8 but are immutable — methods always return new strings, never mutate in place."
    },
    {
      "type": "explanation",
      "title": "Shallow vs. Deep Cloning: Why Spread Is Not Enough",
      "content": "Because assignment copies references, developers often want to **clone** an object to get an independent copy. There are three levels of copying to understand:\n\n**1. Reference copy (alias):** `const b = a;` — single heap object, two references. Any mutation through either is shared.\n\n**2. Shallow clone:** `const b = { ...a };` or `Object.assign({}, a)` — creates a new top-level heap object and copies each of `a`'s own enumerable property *values* into it. If those values are primitives, they are truly copied. If they are themselves references (nested objects or arrays), only the references are copied — `a` and `b` now share the same nested heap objects. Mutating a nested object through `b` still affects `a`.\n\n**3. Deep clone:** `const b = structuredClone(a);` — recursively creates new heap objects for every level of nesting. After a deep clone, `a` and `b` are completely independent trees. `structuredClone()` handles arrays, Maps, Sets, dates, regular expressions, and circular references. It does not handle functions or class instances with custom prototypes.\n\nFor JSON-safe data, `JSON.parse(JSON.stringify(a))` is the classic deep-clone hack — cheap to remember but has pitfalls: it drops `undefined` values, converts `Date` objects to strings, and throws on circular references.\n\nKnowing which level of copying you need is a key skill. Most bugs come from accidentally sharing a reference when you intended independence."
    },
    {
      "type": "code_example",
      "language": "javascript",
      "label": "Example",
      "code": "// ── Shallow clone: nested objects remain shared ───────────────\nconst original = {\n  id: 1,\n  address: { city: 'Tokyo', zip: '100-0001' }\n};\n\nconst shallow = { ...original };\nshallow.id = 999;             // primitive — independent ✓\nshallow.address.city = 'Osaka'; // reference — shared ✗\n\nconsole.log(original.id);             // 1    — unaffected (primitive copied)\nconsole.log(original.address.city);   // 'Osaka' — oops! shared reference\n\n// ── Deep clone: fully independent tree ───────────────────────\nconst deep = structuredClone(original);\ndeep.address.city = 'Kyoto';\nconsole.log(original.address.city);   // 'Osaka' — truly independent now\n\n// ── JSON trick: works but has limitations ─────────────────────\nconst jsonClone = JSON.parse(JSON.stringify(original));\njsonClone.address.city = 'Nagoya';\nconsole.log(original.address.city);   // 'Osaka' — also works\n\n// ── JSON clone drops undefined and converts dates ─────────────\nconst tricky = { a: undefined, b: new Date() };\nconst jsonTricky = JSON.parse(JSON.stringify(tricky));\nconsole.log(jsonTricky);  // { b: '2024-...' }  — 'a' lost, Date is string now",
      "explanation": "Lines 6-11 demonstrate the shallow-clone trap: the top-level id (a primitive) is truly copied, but address (a reference) is shared. Lines 14-16 show structuredClone() producing a fully independent tree. Lines 19-25 show the JSON trick and its two most common failure modes: undefined values are silently dropped and Date objects are serialised to strings."
    },
    {
      "type": "explanation",
      "title": "const, Immutability, and Object.freeze()",
      "content": "`const` is perhaps the most widely misunderstood keyword in JavaScript, and the misunderstanding maps directly to stack-vs-heap confusion.\n\n`const` creates a **binding** — an association between a name and a value — that cannot be **reassigned**. For primitives, that means the value itself cannot change: `const n = 5; n = 6;` throws a `TypeError`. For objects, the *reference* (heap address) stored in the binding is what cannot change. The heap object it points to is entirely mutable: `const arr = [1,2,3]; arr.push(4);` works perfectly because `arr` (the reference) still points to the same heap array — you are mutating the array's *contents*, not reassigning the variable.\n\nTo actually lock heap data, you need explicit immutability tools:\n\n- **`Object.freeze(obj)`**: makes all of `obj`'s own enumerable properties non-writable and non-configurable. In strict mode, attempting to write throws a `TypeError`; in sloppy mode, the write is silently ignored. Freeze is **shallow** — nested objects are not frozen and can still be mutated.\n\n- **Deep freeze**: requires a recursive function that calls `Object.freeze()` on every nested object. There is no built-in deep freeze.\n\n- **Immutable data libraries**: libraries like Immer (used by Redux Toolkit) let you write code that *looks* like mutation but produces new immutable objects behind the scenes, using structural sharing to avoid copying unchanged branches.\n\nThe practical rule: use `const` for everything by default (it prevents accidental reassignment). When you need true immutability, use `Object.freeze()` for simple cases or Immer for complex state trees."
    },
    {
      "type": "diagram",
      "mermaid": "stateDiagram-v2\n  [*] --> Declared\n  Declared --> ConstBinding: const x = obj\n  ConstBinding --> CannotReassign: x = other (TypeError)\n  ConstBinding --> CanMutate: x.prop = value (OK)\n  CanMutate --> Frozen: Object.freeze(x)\n  Frozen --> ThrowsInStrict: x.prop = value\n  Frozen --> SilentIgnore: x.prop = value (sloppy mode)",
      "caption": "const prevents reassigning the binding (the reference); it says nothing about the heap object's mutability. Object.freeze() locks the heap object's own properties, but only one level deep."
    },
    {
      "type": "explanation",
      "title": "Garbage Collection: How the Heap Cleans Itself",
      "content": "In languages like C and C++, the programmer allocates heap memory with `malloc()` and must free it with `free()`. Forgetting to free causes a **memory leak** — heap memory accumulates as the program runs until the OS eventually kills the process. Freeing memory too early causes **use-after-free** bugs — reading or writing deallocated memory, leading to crashes or security vulnerabilities. Manual memory management is powerful but notoriously error-prone.\n\nJavaScript (and Python, Java, Go, C#) solve this with **automatic garbage collection (GC)**. The programmer never calls `free()`; the runtime tracks which heap objects are reachable and periodically reclaims those that are not.\n\n**Mark-and-sweep** is the foundational GC algorithm used (in various refined forms) by V8, SpiderMonkey, and most managed-language runtimes:\n\n1. **Mark phase**: Starting from a set of **GC roots** — global variables, stack variables in active frames, and CPU registers — the collector traverses every reference chain. Every object reachable by following references from roots is marked as live.\n\n2. **Sweep phase**: The collector scans the entire heap. Any object not marked as live is considered garbage and its memory is returned to the free pool.\n\nModern GCs are far more sophisticated than a naive mark-and-sweep:\n- **Generational GC**: most objects die young — they are allocated, used briefly, and quickly become unreachable. V8 splits the heap into a **young generation** (\"nursery\") and an **old generation**. Minor GC runs frequently on the small nursery; major GC (\"full\" or \"old-gen\") runs less often. This dramatically reduces pauses.\n- **Incremental and concurrent GC**: instead of stopping the world for a full sweep, modern GCs do the marking concurrently with program execution, reducing pause times to milliseconds.\n- **Compaction**: after collecting garbage, the GC can move live objects together (compaction), reducing heap fragmentation and improving cache locality.\n\n**What this means for your code**: you can accidentally create **memory leaks in JavaScript** by holding references longer than needed — especially through global variables, event listeners that are never removed, closures that capture more than intended, or long-lived data structures that accumulate entries never removed."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart TD\n  R[\"GC Roots\\n(globals · stack frames · registers)\"] --> O1[\"Object A\\n(reachable — LIVE)\"]\n  O1 --> O2[\"Object B\\n(reachable — LIVE)\"]\n  O1 --> O3[\"Object C\\n(reachable — LIVE)\"]\n  O4[\"Object D\\n(unreachable — GARBAGE)\"]\n  O5[\"Object E\\n(unreachable — GARBAGE)\"]\n  O4 --> O5",
      "caption": "Mark phase: starting from roots, the GC traces all reachable objects (A, B, C) and marks them live. Objects D and E have no path from any root — they are garbage and will be swept. Note D→E is a reference between two garbage objects; neither is saved."
    },
    {
      "type": "explanation",
      "title": "Closures and the Heap: Variables That Outlive Their Frame",
      "content": "Closures are where stack-vs-heap rules intersect in a surprising way. Normally, a function's local variables live on the stack frame and are destroyed when the function returns. But what if an inner function references a local variable from an outer function, and that inner function is passed out and called later? The outer frame is gone, but the inner function still needs that variable.\n\nJavaScript (and most languages with first-class functions) handles this by **escaping** variables that are captured by closures from the stack to the heap. The captured variable — called an **upvalue** in Lua terminology, or a **closed-over variable** in JS — is allocated on the heap as part of the closure object. The closure carries a reference to it.\n\nThis means closures can accidentally keep large objects alive longer than you expect. A classic example: an event listener closure that captures a huge `data` array will prevent that array from being garbage collected as long as the event listener remains registered, even if nothing else references `data`. Removing event listeners when they are no longer needed is not just good practice — it is memory hygiene.\n\nClosures are also why JavaScript can implement private state without classes:\n\n```javascript\nfunction makeCounter() {\n  let count = 0;       // captured on the heap\n  return {\n    increment() { count++; },\n    value()     { return count; }\n  };\n}\nconst c = makeCounter();\nc.increment(); c.increment();\nconsole.log(c.value()); // 2 — count persists on the heap\n```\n\n`count` survives because the returned object's methods hold a reference to it via their closure. The stack frame for `makeCounter()` is gone, but the heap keeps `count` alive as long as the returned object exists."
    },
    {
      "type": "code_example",
      "language": "javascript",
      "label": "Example",
      "code": "// ── Closure capturing a heap variable ────────────────────────\nfunction makeCounter(start = 0) {\n  let count = start;  // 'count' escapes to the heap (captured by closures)\n  return {\n    increment() { count += 1; },\n    decrement() { count -= 1; },\n    reset()     { count = start; },\n    value()     { return count; }\n  };\n}\n\nconst c = makeCounter(10);\nc.increment();\nc.increment();\nc.decrement();\nconsole.log(c.value()); // 11\n\n// ── Memory-leak pattern: closure capturing too much ───────────\nfunction registerHandler(bigData) {\n  const summary = bigData.length; // only need the length\n  // Bad: captures the entire bigData array through the closure\n  document.addEventListener('click', () => {\n    console.log('data length was', bigData.length);\n  });\n  // bigData cannot be GC'd as long as the listener is registered!\n}\n\n// Better: extract only what you need, avoid capturing the full object\nfunction registerHandlerFixed(bigData) {\n  const summary = bigData.length;\n  document.addEventListener('click', () => {\n    console.log('data length was', summary); // bigData can now be GC'd\n  });\n}\n\n// ── Stack overflow from deep recursion ────────────────────────\nfunction factorial(n) {\n  if (n <= 1) return 1;\n  return n * factorial(n - 1);  // each call pushes a new stack frame\n}\nconsole.log(factorial(10));   // 3628800 — fine\n// factorial(100000);          // RangeError: Maximum call stack size exceeded\n\n// Iterative version avoids stack growth:\nfunction factorialIter(n) {\n  let result = 1;\n  for (let i = 2; i <= n; i++) result *= i;\n  return result;\n}\nconsole.log(factorialIter(100000)); // works — O(1) stack space",
      "explanation": "The makeCounter closure keeps 'count' alive on the heap even after makeCounter() returns — demonstrating how closures extend object lifetimes. The registerHandler example shows a real memory-leak pattern: unnecessarily capturing a large object in a long-lived event listener. The factorial example contrasts recursive (stack-growing) vs. iterative (O(1) stack) implementations — a critical pattern for processing deep structures."
    },
    {
      "type": "explanation",
      "title": "Value vs. Reference in Function Arguments: Pass by Value of the Reference",
      "content": "JavaScript is technically **pass-by-value** — always. But for reference types, the value being passed is *the reference itself* (the memory address). This nuance trips up many developers.\n\nWhen you pass a primitive, the function receives a copy of the number/string/boolean. When you pass an object, the function receives a copy of the reference (address) — so the function can mutate the heap object the reference points to, but it cannot make the caller's variable point to a different object.\n\n```javascript\nfunction tryToReplace(obj) {\n  obj = { brand: 'new object' };  // reassigns local copy of reference\n}                                  // caller's reference unchanged\n\nconst original = { value: 1 };\ntryToReplace(original);\nconsole.log(original); // { value: 1 } — caller unaffected\n```\n\n```javascript\nfunction mutate(obj) {\n  obj.value = 99;  // mutates the shared heap object\n}\n\nconst item = { value: 1 };\nmutate(item);\nconsole.log(item.value); // 99 — caller sees the change\n```\n\nThis is sometimes called **pass-by-sharing** or **call-by-object-sharing** to distinguish it from true pass-by-reference (where the caller's variable itself could be repointed). Languages like C++ have actual pass-by-reference (`void f(int& x)`) which can rebind the caller's variable; JavaScript does not."
    },
    {
      "type": "callout",
      "variant": "info",
      "content": "**Historical and cross-language context:** The stack-and-heap separation is not specific to JavaScript. In **C/C++**, every local variable goes on the stack by default; you use `malloc()`/`new` explicitly for heap allocation and `free()`/`delete` to reclaim it. In **Rust**, the ownership and borrow checker enforces at compile time that heap data is freed exactly once and never used after freeing — eliminating both memory leaks and use-after-free bugs without a GC, at the cost of a steeper learning curve. In **Python**, all objects — even small integers and strings — are heap-allocated reference-counted objects (CPython uses reference counting plus a cycle collector). In **Go**, the compiler performs **escape analysis**: if a local variable's address escapes the function (e.g., is returned or stored in a global), the compiler automatically allocates it on the heap; otherwise it stays on the stack. The trade-offs between GC, reference counting, and manual management are a major dimension along which languages differ."
    },
    {
      "type": "callout",
      "variant": "tip",
      "content": "**Interview insight — what interviewers actually want to hear:**\n\nWhen asked 'what is the difference between the stack and the heap?', lead with **lifetime and management**, not just speed:\n\n- Stack: automatic, deterministic lifetime tied to function-call scope; freed on return; LIFO; fast; fixed size per thread.\n- Heap: indefinite lifetime; freed when GC detects no live references; random access; slower allocation; much larger.\n\nThen connect to JavaScript specifics: primitives are value types (copied on assignment), objects are reference types (reference is copied, heap data is shared). Mention that `const` prevents reference reassignment but does not freeze heap data.\n\nBonus points: mention closures as the mechanism that moves stack variables to the heap when they need to outlive their frame. Mention generational GC as why creating many short-lived objects can still be relatively efficient in V8.\n\nCommon wrong answer to avoid: 'the stack is fast and the heap is slow, therefore use the stack' — in JavaScript you do not choose where values go; the engine decides based on type. The insight is understanding the *consequences* of that choice."
    },
    {
      "type": "key_takeaways",
      "items": [
        "The **stack** is a small, fast, LIFO memory region. Each function call pushes a frame containing parameters, local primitives, and a return address. The frame is reclaimed instantly when the function returns — stack allocation costs just one pointer move.",
        "The **heap** is a large, flexible region for data that must outlive a single function call. Objects are allocated here and persist until the garbage collector determines no live reference reaches them.",
        "JavaScript **primitives** (number, string, boolean, null, undefined, symbol, bigint) are stored by value — assignment copies the raw value and the copy is fully independent.",
        "JavaScript **objects, arrays, and functions** are heap-allocated reference types — variables hold memory addresses. Assigning or passing an object copies the address, creating an alias; mutations through any alias affect the shared heap object.",
        "`const` prevents **reference reassignment** only. The heap object a `const` variable points to is freely mutable. Use `Object.freeze()` for shallow immutability or `structuredClone()` for a fully independent deep copy.",
        "**Closures** capture variables onto the heap so they outlive the function frame that created them. This enables private state and functional patterns but can cause memory leaks if closures unintentionally keep large objects reachable.",
        "The **garbage collector** reclaims heap memory using reachability: starting from GC roots (globals, stack frames), it marks all reachable objects as live and sweeps everything else. Modern generational, incremental GCs in V8 keep pause times under a millisecond in most cases."
      ]
    }
  ],
  "visualizer": {
    "type": "none",
    "caption": ""
  },
  "quiz": [
    {
      "id": "q1",
      "question": "Where are a function's local primitive variables stored during execution?",
      "type": "mcq",
      "options": [
        "On the heap, managed by the garbage collector",
        "In the stack frame for that function call",
        "In a global memory region shared by all functions",
        "In CPU registers only, never in RAM"
      ],
      "correct": 1,
      "explanation": "Each function call pushes a stack frame that holds its local primitives and parameters. The frame is popped and freed the instant the function returns — no GC involvement needed."
    },
    {
      "id": "q2",
      "question": "After `const user1 = { score: 10 }; const user2 = user1; user2.score = 99;`, what is `user1.score`?",
      "type": "mcq",
      "options": [
        "10 — user1 is protected by const",
        "99 — user1 and user2 reference the same heap object",
        "undefined — user1 was reassigned",
        "A TypeError is thrown because user2 is const"
      ],
      "correct": 1,
      "explanation": "user1 and user2 both hold the same heap address. Mutating through user2 changes the shared heap object. const only prevents reassigning the reference — it does not freeze the heap data."
    },
    {
      "id": "q3",
      "question": "What does `structuredClone(obj)` do that `{ ...obj }` does not?",
      "type": "mcq",
      "options": [
        "It freezes the resulting object so it cannot be mutated",
        "It recursively deep-copies all nested objects, producing a fully independent tree",
        "It copies the prototype chain and class methods",
        "It converts the object to JSON and back"
      ],
      "correct": 1,
      "explanation": "Spread ({ ...obj }) is a shallow clone — nested objects are still shared references. structuredClone() recursively copies all levels, so the clone and the original are completely independent. It also handles circular references, Maps, Sets, and Dates correctly."
    },
    {
      "id": "q4",
      "question": "What does the garbage collector use to decide whether a heap object can be reclaimed?",
      "type": "mcq",
      "options": [
        "How long the object has been alive in milliseconds",
        "The size of the object in bytes",
        "Whether any live reference chain from a GC root reaches the object",
        "Whether the variable was declared with let or const"
      ],
      "correct": 2,
      "explanation": "JavaScript's GC uses reachability (mark-and-sweep). An object is collected only when no live path from a root — global, stack variable, or active closure — leads to it. Age and size do not determine collectability."
    },
    {
      "id": "q5",
      "question": "Why does `const arr = [1, 2, 3]; arr.push(4);` not throw a TypeError?",
      "type": "mcq",
      "options": [
        "Arrays are special — const does not apply to them",
        "const only prevents reassigning the reference; mutating the heap array through push() is allowed",
        "push() creates a new array, so arr is not modified",
        "const is only enforced in strict mode"
      ],
      "correct": 1,
      "explanation": "const prevents arr = somethingElse (reassigning the binding). It does not prevent modifying the heap array that arr points to. push() mutates the existing array in place, which is perfectly legal."
    },
    {
      "id": "q6",
      "question": "What is the most likely cause of a 'Maximum call stack size exceeded' (RangeError) in JavaScript?",
      "type": "mcq",
      "options": [
        "Allocating a very large array on the heap",
        "Infinite or very deep recursion exhausting the call stack",
        "Mutating a const variable",
        "Calling JSON.parse on an invalid string"
      ],
      "correct": 1,
      "explanation": "Each function call pushes a stack frame. Unbounded recursion keeps pushing frames until the thread's stack size limit (typically 1-8 MB) is exceeded — a stack overflow."
    },
    {
      "id": "q7",
      "question": "A closure captures a large array in an event listener that is never removed. What memory problem can this cause?",
      "type": "mcq",
      "options": [
        "Stack overflow, because the array uses too much stack space",
        "A memory leak, because the closure keeps the array reachable and the GC cannot collect it",
        "A TypeError, because event listeners cannot capture variables",
        "No problem — the GC detects this pattern and collects the array anyway"
      ],
      "correct": 1,
      "explanation": "As long as the event listener is registered, the closure holds a reference to the array, making it reachable from a GC root (the event target). The GC will never collect it, causing heap memory to grow indefinitely — a classic JavaScript memory leak."
    },
    {
      "id": "q8",
      "question": "You call `function tryReplace(obj) { obj = { x: 99 }; }` with an object argument. After the call, what happens to the caller's variable?",
      "type": "mcq",
      "options": [
        "The caller's variable now points to the new object { x: 99 }",
        "The caller's variable is unchanged — only the local copy of the reference was reassigned",
        "The original object is mutated to have x: 99",
        "A TypeError is thrown because obj is const inside the function"
      ],
      "correct": 1,
      "explanation": "JavaScript passes a copy of the reference. Reassigning obj inside the function only changes the local copy; the caller's variable still holds the original heap address. To affect the caller, you must mutate the object's properties, not reassign obj."
    },
    {
      "id": "q9",
      "question": "Which JavaScript primitive is stored by value when assigned to a new variable?",
      "type": "mcq",
      "options": [
        "An array []",
        "A plain object {}",
        "A boolean true/false",
        "A function () => {}"
      ],
      "correct": 2,
      "explanation": "Booleans are primitives — they are copied by value. Arrays, plain objects, and functions are all heap-allocated reference types; assignment copies the reference, not the data."
    },
    {
      "id": "q10",
      "question": "After `const cfg = Object.freeze({ host: 'localhost', port: 3000 }); cfg.port = 9999;`, what is `cfg.port` in strict mode?",
      "type": "mcq",
      "options": [
        "9999 — Object.freeze only affects future Object.freeze calls",
        "3000 — the write is silently ignored (sloppy mode) or throws TypeError (strict mode)",
        "undefined — freeze removes all property values",
        "A ReferenceError is thrown because cfg is const"
      ],
      "correct": 1,
      "explanation": "Object.freeze() makes the object's own properties non-writable. In sloppy mode the write is silently ignored and cfg.port remains 3000. In strict mode a TypeError is thrown. Either way, cfg.port is 3000 if the code continues — not 9999."
    }
  ]
}
