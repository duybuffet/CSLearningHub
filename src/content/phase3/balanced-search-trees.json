{
  "id": "phase3_balanced-search-trees",
  "title": "Balanced Search Trees",
  "phase": 3,
  "week": 10,
  "icon": "stack",
  "tagline": "AVL rotations, Red-Black trees, and B-trees — the self-balancing structures that keep O(log n) guarantees alive under insertion and deletion.",
  "readTime": "~22 min",
  "sections": [
    {
      "type": "intro",
      "content": "A plain binary search tree (BST) is one of the most elegant data structures in computer science: every node's left child is smaller, every right child is larger, and search, insert, and delete all run in O(h) where h is the tree's height. That feels great until you notice the fine print — h is not guaranteed to be O(log n). Insert the sequence 1, 2, 3, 4, 5 in sorted order and you get a **degenerate BST** that looks exactly like a linked list, with h = n and O(n) operations. This is not a theoretical edge case: real-world data is often nearly sorted (log files, timestamps, auto-incrementing IDs, alphabetical imports), and attackers can craft adversarial insert sequences to deliberately degrade a BST into linear time.\n\n**Balanced search trees** solve this by self-adjusting on every insert and delete to maintain a height that is always O(log n), regardless of input order. The tree does not merely hope to stay balanced — it enforces it with local structural fixes called **rotations** (or node splits, in disk-oriented trees). This chapter builds the complete picture: why balance matters, exactly how the two dominant in-memory structures (AVL trees and Red-Black trees) enforce their invariants, and how B-trees and B+ trees extend the idea to the massive branching factors needed by databases and file systems.\n\nBy the end of this chapter you will understand: (1) what makes a BST degenerate and why it matters, (2) the four AVL rotation cases and how to execute them, (3) the five Red-Black invariants and how they bound height at 2 log n, (4) why AVL is faster for reads while Red-Black is faster for writes, (5) how B-trees pack hundreds of keys per node to minimise disk I/O, and (6) how B+ trees add linked leaves to enable efficient range queries — the index structure that powers every relational database you have ever used."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart LR\n  A[\"Sorted insert: 1,2,3,4,5\"] --> B[\"Degenerate BST\\nheight = n\\nO(n) ops\"]\n  C[\"AVL / RB Tree\\nsame keys\"] --> D[\"Balanced BST\\nheight = O(log n)\\nO(log n) ops\"]\n  D --> E[\"B-Tree\\nfor disk storage\\nheight = O(log_M n)\"]",
      "caption": "The core problem: sorted insertion degenerates a BST to a linked list. Balanced search trees prevent this regardless of input order."
    },
    {
      "type": "analogy",
      "title": "The Self-Levelling Shelf",
      "content": "Imagine a very long bookshelf divided into two sides. Each time you add a book you must put it on the left side if it comes alphabetically before the centre book, or on the right side if it comes after. This works great when books arrive in random order — both sides stay roughly even. But if you add books A, B, C, D, E, F in order, every book ends up on the right side of the previous one and your 'shelf' becomes a single leaning tower.\n\nA balanced search tree is like having a tidy librarian who, after you place each book, steps back, inspects the shelf, and makes at most one or two small **swaps** — moving a book up or down — to keep both sides roughly equal in length. The swaps are so local and fast (O(1) pointer changes) that the total overhead per operation is still O(log n).\n\nThe librarian's rulebook differs by tree type. An AVL librarian is a perfectionist: the two sides may differ in height by at most one book. A Red-Black librarian is more relaxed: she paints books either red or black, and as long as every path from top to bottom has the same number of black books — and no two red books sit adjacent — she is happy. A B-tree librarian manages an enormous warehouse with very wide shelves: instead of swapping books, she splits a full shelf into two when it overflows, keeping all shelves at exactly the same depth from the ground."
    },
    {
      "type": "explanation",
      "title": "Why Height Is Everything: The Degenerate BST Problem",
      "content": "Before diving into solutions, it is worth making the degenerate BST problem concrete. A BST with n nodes has height h somewhere between ⌊log₂ n⌋ (perfect binary tree — best case) and n − 1 (linear chain — worst case). Every operation — search, insert, delete — traverses from root to a leaf, so its cost is O(h).\n\nFor a random permutation of n distinct keys, the expected height of a BST is about 4.311 ln n, which is O(log n). So random insertion is fine. The danger is **structured input**: sorted ascending, sorted descending, alternating extremes, or any other pattern that consistently sends new nodes to one side. Given that auto-increment primary keys, timestamped events, alphabetical imports, and similar patterns are common in production, relying on 'probably random' is reckless.\n\nThe vulnerability was exploited in real systems. PHP's hash table had a worst-case collision attack in 2011 that caused DoS by triggering O(n) hash chain behaviour — a similar structural issue. BST libraries used in compilers and runtimes have been attacked the same way. **Self-balancing is not a micro-optimisation — it is a correctness guarantee.**\n\nThe key insight that makes balancing tractable is that you never need to rebuild the entire tree. A **rotation** — a local rewiring of three nodes and a few pointers — can reduce height imbalance by one while preserving the BST ordering property exactly. Compose a small number of rotations after each insert or delete, and you can maintain O(log n) height forever."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart TD\n  R[\"Root: 1\"]\n  R --> N2[\"2\"]\n  N2 --> N3[\"3\"]\n  N3 --> N4[\"4\"]\n  N4 --> N5[\"5\"]",
      "caption": "Inserting 1, 2, 3, 4, 5 in sorted order into a plain BST: a right-skewed chain of height 5. Every search traverses all 5 nodes — O(n)."
    },
    {
      "type": "explanation",
      "title": "AVL Trees: Strict Balance via Rotation",
      "content": "AVL trees were invented by Georgy Adelson-Velsky and Evgenii Landis in 1962 — the first self-balancing BST ever published. Their key idea: attach a **balance factor** to every node, defined as `height(left subtree) − height(right subtree)`. A valid AVL node must have a balance factor of −1, 0, or +1. Any deviation — a balance factor of ±2 — triggers a rebalancing rotation.\n\n**Computing height:** Each node stores its height explicitly (an integer). The height of a null node is 0 (or −1, depending on convention — just be consistent). After any structural change, you recompute heights bottom-up along the path from the modified node to the root. This is O(log n) total.\n\n**The four imbalance cases** each correspond to a distinct rotation or double rotation:\n\n**Case 1 — Left-Left (LL):** The imbalanced node `z` is left-heavy (balance factor +2), and its left child `y` is also left-heavy or neutral (balance factor ≥ 0). The new node was inserted into `y`'s left subtree. Fix: a single **right rotation** on `z`. `y` becomes the new subtree root, `z` drops to `y`'s right child, and `y`'s old right subtree `T2` becomes `z`'s left child. BST order is preserved because all keys in `T2` are between `y` and `z`.\n\n**Case 2 — Right-Right (RR):** Mirror of LL. `z` is right-heavy, `y` is its right child. Fix: single **left rotation** on `z`.\n\n**Case 3 — Left-Right (LR):** `z` is left-heavy, but `y` (its left child) is right-heavy (balance factor −1). The new node was inserted into `y`'s right subtree. A single right rotation on `z` would not fix this. Fix: first rotate **left on `y`** (converting LR to LL), then rotate **right on `z`**. Two rotations total.\n\n**Case 4 — Right-Left (RL):** Mirror of LR. Fix: rotate right on `z`'s right child, then rotate left on `z`.\n\nAfter rebalancing, the height of the subtree that was previously imbalanced is the same as before the insert — so the fix never propagates further up the tree for insertion. For deletion, however, height can decrease, which may trigger more rotations up to the root: O(log n) rotations in the worst case for deletion."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart TD\n  Before[\"Before: LL case\"]\n  Before --> Z[\"z bf=+2\"]\n  Z --> Y[\"y bf=+1\"]\n  Y --> X[\"x new node\"]\n  Y --> T2[\"T2\"]\n  Z --> T3[\"T3\"]\n  After[\"After: right rotation\"]\n  After --> Y2[\"y new root\"]\n  Y2 --> X2[\"x\"]\n  Y2 --> Z2[\"z\"]\n  Z2 --> T22[\"T2\"]\n  Z2 --> T32[\"T3\"]",
      "caption": "AVL right rotation for the LL case: y rises to be the new subtree root, z drops to y's right. Subtrees T2 and T3 move with their parents. BST ordering is preserved because all keys in T2 lie between y and z."
    },
    {
      "type": "code_example",
      "language": "javascript",
      "label": "Example",
      "code": "// AVL Tree — complete insert with rebalancing\nclass AVLNode {\n  constructor(key) {\n    this.key    = key;\n    this.left   = null;\n    this.right  = null;\n    this.height = 1;  // leaf height starts at 1\n  }\n}\n\nclass AVLTree {\n  #root = null;\n\n  // Height of a node (null = 0)\n  #h(n) { return n ? n.height : 0; }\n\n  // Balance factor: positive = left-heavy, negative = right-heavy\n  #bf(n) { return n ? this.#h(n.left) - this.#h(n.right) : 0; }\n\n  // Recompute height from children\n  #updateH(n) { n.height = 1 + Math.max(this.#h(n.left), this.#h(n.right)); }\n\n  // LL fix: rotate right at y\n  //         y                    x\n  //        / \\      →          / \\\n  //       x   T3             T1   y\n  //      / \\                     / \\\n  //     T1  T2                  T2  T3\n  #rotateRight(y) {\n    const x  = y.left;\n    const T2 = x.right;\n    x.right  = y;   // x rises\n    y.left   = T2;  // T2 rehomed\n    this.#updateH(y);\n    this.#updateH(x);\n    return x; // new subtree root\n  }\n\n  // RR fix: rotate left at x\n  #rotateLeft(x) {\n    const y  = x.right;\n    const T2 = y.left;\n    y.left   = x;\n    x.right  = T2;\n    this.#updateH(x);\n    this.#updateH(y);\n    return y;\n  }\n\n  // Rebalance after insert/delete, returning new subtree root\n  #balance(node) {\n    this.#updateH(node);\n    const bf = this.#bf(node);\n\n    if (bf > 1) {                         // left-heavy\n      if (this.#bf(node.left) < 0)        // LR case: left child is right-heavy\n        node.left = this.#rotateLeft(node.left);  // reduce to LL\n      return this.#rotateRight(node);     // LL case\n    }\n\n    if (bf < -1) {                        // right-heavy\n      if (this.#bf(node.right) > 0)       // RL case: right child is left-heavy\n        node.right = this.#rotateRight(node.right); // reduce to RR\n      return this.#rotateLeft(node);      // RR case\n    }\n\n    return node; // already balanced\n  }\n\n  #insert(node, key) {\n    if (!node) return new AVLNode(key);                      // base case\n    if      (key < node.key) node.left  = this.#insert(node.left,  key);\n    else if (key > node.key) node.right = this.#insert(node.right, key);\n    else                     return node;                    // duplicate: no-op\n    return this.#balance(node); // fix balance on the way back up\n  }\n\n  insert(key) { this.#root = this.#insert(this.#root, key); }\n  height()    { return this.#h(this.#root); }\n\n  // Iterative search — no balance needed, just BST property\n  search(key) {\n    let node = this.#root;\n    while (node) {\n      if      (key === node.key) return true;\n      else if (key  <  node.key) node = node.left;\n      else                       node = node.right;\n    }\n    return false;\n  }\n}\n\n// Demonstrate that sorted insertion still yields O(log n) height\nconst avl = new AVLTree();\n[1, 2, 3, 4, 5, 6, 7].forEach(k => avl.insert(k));\nconsole.log('height after inserting 1-7 in order:', avl.height()); // 3, not 7\nconsole.log(avl.search(5)); // true\nconsole.log(avl.search(9)); // false\n\n// In a plain BST, inserting 1-7 sorted gives height 7.\n// AVL tree rebalances to height ceil(log2(8)) = 3.",
      "explanation": "The crucial pattern is that `#insert` returns the (possibly new) subtree root after calling `#balance`. This 'returning up the recursion stack' is how height fixes propagate. `#balance` unifies all four cases: it checks whether we have a double rotation (LR or RL) by examining the child's balance factor before applying the outer rotation. After inserting 7 sorted keys a plain BST would have height 7; the AVL tree caps it at 3."
    },
    {
      "type": "explanation",
      "title": "Red-Black Trees: Relaxed Balance via Colour Invariants",
      "content": "Red-Black trees were introduced by Rudolf Bayer in 1972 (as 'symmetric binary B-trees') and refined by Guibas and Sedgewick in 1978. They relax the strict AVL balance requirement in exchange for far fewer rotations per write operation — making them the preferred choice for general-purpose sorted maps in standard libraries.\n\n**The five Red-Black invariants** (every valid Red-Black tree satisfies all five simultaneously):\n1. Every node is either **red** or **black**.\n2. The **root** is black.\n3. Every **null leaf** (sentinel) is black.\n4. If a node is **red**, both its children are **black** (no two consecutive red nodes).\n5. Every path from a given node down to any null leaf passes through the **same number of black nodes** — this number is called the **black-height** of that node.\n\nThese invariants together guarantee that the longest root-to-leaf path is at most **twice** the shortest path. The shortest path contains only black nodes (length = black-height `bh`). The longest path alternates red and black nodes (length = 2 × bh). Since a binary tree with all paths of length at least bh has at least 2^bh − 1 internal nodes, we get bh ≤ log₂(n+1), and thus height ≤ 2 log₂(n+1). This is approximately 1.39× worse than AVL's 1.44 log₂ n bound in practice, but the difference in absolute tree height for any realistic n is small (e.g., for n = 1,000,000: AVL ≈ 28 levels, Red-Black ≤ 40 levels).\n\n**Rebalancing after insert:** The new node is always inserted red (to preserve black-height). If its parent is black, done. If its parent is red, we have two consecutive reds — an invariant violation. The fix depends on the colour of the 'uncle' node (the sibling of the parent):\n- **Uncle is red:** Recolour parent and uncle to black, grandparent to red. Push the potential violation up to the grandparent. This may propagate up to O(log n) recolourings, but at most 2 rotations total.\n- **Uncle is black:** Apply one or two rotations, recolour — and the violation is fully resolved in O(1) additional steps. At most 2 rotations per insert.\n\n**Rebalancing after delete:** Deletion can create a 'double black' node — a node that accounts for two units of black on some paths. Resolving double black requires examining the sibling's colour and its children's colours, and applying up to 3 rotations total. The analysis is more complex but the bound is tight."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart TD\n  Root[\"B: 20 (black)\"] --> L[\"R: 10 (red)\"]\n  Root --> R[\"B: 30 (black)\"]\n  L --> LL[\"B: 5 (black)\"]\n  L --> LR[\"B: 15 (black)\"]\n  R --> RL[\"B: 25 (black)\"]\n  R --> RR[\"B: 35 (black)\"]",
      "caption": "A valid Red-Black tree. Every root-to-null path has the same black-height (3 black nodes). No two consecutive red nodes exist. The root is black. This tree has height 3 and holds 6 keys."
    },
    {
      "type": "explanation",
      "title": "AVL vs Red-Black: When to Choose Which",
      "content": "Both AVL trees and Red-Black trees provide O(log n) search, insert, and delete. The practical difference lies in **constant factors and rotation counts** — not Big-O complexity class.\n\n**Height comparison for n = 1,000,000 nodes:**\n- Perfect BST (theoretical best): height ≈ 20\n- AVL tree: height ≤ 1.44 × log₂(n) ≈ 29\n- Red-Black tree: height ≤ 2 × log₂(n+1) ≈ 40\n\nAVL's stricter invariant produces a shorter, flatter tree. A shorter tree means fewer node comparisons per search — roughly 30% faster reads in practice. This makes AVL the choice for **read-heavy, in-memory** workloads such as compiler symbol tables and database query plan caches.\n\n**Rotation counts per operation:**\n- AVL insert: at most 2 rotations (one double rotation at the lowest imbalance point).\n- AVL delete: up to O(log n) rotations, potentially all the way to the root.\n- Red-Black insert: at most 2 rotations, plus O(log n) recolourings (cheap).\n- Red-Black delete: at most 3 rotations total.\n\nThe bounded rotation count on writes makes Red-Black trees dramatically better for **write-heavy** workloads. This is why Java's `TreeMap` and `TreeSet`, C++'s `std::map` and `std::set`, and the Linux kernel's Completely Fair Scheduler (CFS) all use Red-Black trees. The Linux process scheduler must insert and delete tasks at extremely high rates; even a small constant-factor overhead per delete is unacceptable.\n\n**A useful heuristic for interviews and system design:** If you need an ordered map and reads dominate, AVL is theoretically better. If writes (inserts and deletes) are frequent or balanced with reads, use Red-Black (which is what your language's standard library almost certainly provides). For disk-based storage, neither — use B-trees."
    },
    {
      "type": "code_concept",
      "language": "javascript",
      "label": "Example",
      "code": "// Red-Black Tree invariant checker — verify a tree is valid\n// (Useful for testing; this is NOT the tree itself, just the checker)\n\nconst RED   = 'red';\nconst BLACK = 'black';\n\n// Returns the black-height if valid, throws if invariant violated\nfunction checkRBInvariants(node, isRoot = true) {\n  // Null nodes are black leaves (base case)\n  if (node === null) return 1; // counts as 1 black node on path\n\n  // Invariant 2: root must be black\n  if (isRoot && node.color !== BLACK)\n    throw new Error(`Root node ${node.key} must be black`);\n\n  // Invariant 4: red node cannot have red children\n  if (node.color === RED) {\n    if (node.left  && node.left.color  === RED)\n      throw new Error(`Red-Red violation at ${node.key} -> ${node.left.key}`);\n    if (node.right && node.right.color === RED)\n      throw new Error(`Red-Red violation at ${node.key} -> ${node.right.key}`);\n  }\n\n  // Invariant 5: left and right black-heights must match\n  const leftBH  = checkRBInvariants(node.left,  false);\n  const rightBH = checkRBInvariants(node.right, false);\n  if (leftBH !== rightBH)\n    throw new Error(\n      `Black-height mismatch at node ${node.key}: left=${leftBH}, right=${rightBH}`\n    );\n\n  // This node's contribution to the black-height of its parent\n  return leftBH + (node.color === BLACK ? 1 : 0);\n}\n\n// Example: build a minimal valid RB tree by hand and verify it\nconst root  = { key: 20, color: BLACK, left: null, right: null };\nroot.left   = { key: 10, color: RED,   left: null, right: null };\nroot.right  = { key: 30, color: RED,   left: null, right: null };\n// Null children of 10 and 30 are black sentinel leaves — OK\ntry {\n  const bh = checkRBInvariants(root);\n  console.log('Valid RB tree, black-height:', bh); // Valid RB tree, black-height: 2\n} catch(e) {\n  console.error('Invariant violated:', e.message);\n}\n\n// Now deliberately break invariant 4 (two consecutive reds)\nroot.left.left = { key: 5, color: RED, left: null, right: null };\ntry {\n  checkRBInvariants(root);\n} catch(e) {\n  console.error('Caught:', e.message); // Caught: Red-Red violation at 10 -> 5\n}",
      "explanation": "The invariant checker walks the tree once, verifying the colour rules and computing the black-height at each node. The recursive structure mirrors how the actual rebalancing code propagates information: every call returns the black-height of its subtree, and mismatches surface the exact location of the violation. This pattern is the basis for every RB-tree test suite."
    },
    {
      "type": "explanation",
      "title": "B-Trees: Balancing on Disk",
      "content": "AVL and Red-Black trees are elegant for **in-memory** data. But databases and file systems operate on **disk**, where the rules are completely different. A single disk seek takes roughly 5–10 milliseconds — about 10 million times slower than a RAM access. An operation that performs 30 node reads from a binary tree (as O(log₂ n) requires for n = 1 billion) incurs 30 disk seeks, which at 10ms each means 300ms — half a second just for one lookup. This is completely unacceptable for a database index.\n\nThe solution is to change the **branching factor**. Instead of each node having at most 2 children, let each node have up to **M** children. A **B-tree of order M** satisfies:\n- Every node holds at most **M − 1 keys** and **M child pointers**.\n- Every non-root node holds at least **⌈M/2⌉ − 1 keys**.\n- The **root** holds at least 1 key (or is a leaf).\n- All **leaves are at the same depth** — B-trees are perfectly height-balanced.\n- Keys within a node are sorted; child pointers separate consecutive key ranges.\n\nWith a disk page of 4 KB and keys of 8 bytes plus pointers of 8 bytes, you can fit approximately **M = 200** entries per node. A 4-level B-tree can therefore index **200³ ≈ 8 million records** while accessing at most **4 disk pages** per lookup. Compare this to a binary tree requiring ⌈log₂(8,000,000)⌉ = 23 disk reads. The branching factor is the entire game.\n\n**B-tree insert:** Insert the key into the correct leaf. If the leaf overflows (more than M − 1 keys), **split** it: push the median key up to the parent and create a new sibling node. The split may propagate up if the parent also overflows. In the worst case, a chain of splits reaches the root, increasing tree height by one — the only way a B-tree grows taller.\n\n**B-tree delete:** Find and remove the key. If the node becomes under-full (fewer than ⌈M/2⌉ − 1 keys), try to **borrow** a key from an adjacent sibling through the parent. If the sibling is also too small, **merge** the node, sibling, and the separating parent key into one node, then repeat up the tree. This is the mirror of insertion.\n\n**Height bound:** A B-tree with n keys has height at most log_{⌈M/2⌉}((n+1)/2). For M = 200, this is log₁₀₀(n/2). For n = 1 billion: log₁₀₀(500,000,000) ≈ 4.4 — so about 5 levels. Five disk reads to find any record among a billion."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart TD\n  Root[\"Root: [30, 60]\"]\n  Root --> C1[\"[10, 20]\"]\n  Root --> C2[\"[40, 50]\"]\n  Root --> C3[\"[70, 80, 90]\"]\n  C1 --> L1[\"leaf: [5,8]\"]\n  C1 --> L2[\"leaf: [15,18]\"]\n  C1 --> L3[\"leaf: [25,28]\"]\n  C2 --> L4[\"leaf: [35,38]\"]\n  C2 --> L5[\"leaf: [45,48]\"]\n  C2 --> L6[\"leaf: [55,58]\"]",
      "caption": "A B-tree of order 4: each internal node holds 2-3 keys and 3-4 child pointers. All leaves are at the same depth. Key 30 routes to C1 or C2; key 60 routes to C2 or C3."
    },
    {
      "type": "explanation",
      "title": "B+ Trees: Range Queries and the Linked Leaf Layer",
      "content": "Every major relational database — PostgreSQL, MySQL InnoDB, Oracle, SQLite — uses a **B+ tree** variant rather than a plain B-tree. The difference is subtle but enormously impactful for query performance.\n\n**In a plain B-tree**, both internal nodes and leaf nodes store actual record data (or pointers to records). This means that when you want to retrieve all records where `age BETWEEN 25 AND 40`, you find each qualifying record independently with a separate root-to-leaf traversal — there is no direct path from one qualifying record to the next.\n\n**In a B+ tree:**\n- **Internal nodes** store only **keys** (no record data). This allows them to hold more keys, increasing the branching factor and further reducing height.\n- **Leaf nodes** store all the actual records (or record pointers) and are connected as a **doubly-linked list** sorted by key.\n- Every key that appears in an internal node also appears in a leaf (as a duplicate copy).\n\nThis architecture makes **range queries** extremely efficient. To find all employees aged 25–40:\n1. Traverse from root to the leaf containing key 25 — O(log n) disk reads.\n2. Walk the linked list of leaves rightward until you pass key 40 — O(k/L) reads where k is the number of qualifying records and L is the leaf capacity.\n\nCompare this to B-tree: each qualifying record requires a separate O(log n) traversal. For a range query returning k records, B+ tree costs O(log n + k/L) while B-tree costs O(k log n) — an enormous practical difference when k is large.\n\n**Why internal nodes store only keys:** A 4KB page holding only 8-byte keys plus pointers fits about 250 entries. A page holding full records (say, 100 bytes each) fits only about 40. The larger branching factor means fewer levels, fewer disk reads per lookup. Everything in database indexing comes back to minimising disk I/O.\n\n**Real-world indexes:** PostgreSQL's default index type is B-tree (which in practice is B+ tree). MySQL InnoDB uses a clustered B+ tree where the primary key index IS the table — the leaves contain the full row data. Secondary indexes point to the primary key rather than the physical row, enabling online structural changes. SQLite likewise uses B+ trees for both indexes and table storage."
    },
    {
      "type": "diagram",
      "mermaid": "flowchart LR\n  I1[\"Internal: [30, 60]\"] --> L1\n  I1 --> L2\n  I1 --> L3\n  L1[\"Leaf 1\\nkeys: 10,20,25\"] --> L2\n  L2[\"Leaf 2\\nkeys: 30,40,50\"] --> L3\n  L3[\"Leaf 3\\nkeys: 60,70,80\"]\n  L3 -.->|\"doubly linked\"| L2\n  L2 -.->|\"doubly linked\"| L1",
      "caption": "B+ tree: internal nodes hold only keys for routing; all records live in leaves connected as a doubly-linked list. A range scan finds the first key via the tree, then walks the linked list — no backtracking through internal nodes."
    },
    {
      "type": "code_concept",
      "language": "javascript",
      "label": "Example",
      "code": "// B+ Tree conceptual simulation — range query on a linked leaf layer\n// (Simplified: shows the linked-leaf scan pattern, not full disk-page management)\n\nclass Leaf {\n  constructor(keys, records) {\n    this.keys    = keys;    // sorted array of keys\n    this.records = records; // corresponding record data\n    this.next    = null;    // linked list pointer to next leaf\n    this.prev    = null;\n  }\n}\n\n// Build a tiny B+ tree leaf layer for demonstration\nconst leaf1 = new Leaf([5, 10, 15],  ['alice', 'bob',   'carol']);\nconst leaf2 = new Leaf([20, 25, 30], ['dave',  'eve',   'frank']);\nconst leaf3 = new Leaf([35, 40, 45], ['grace', 'heidi', 'ivan']);\nconst leaf4 = new Leaf([50, 55, 60], ['judy',  'karl',  'laura']);\n\n// Link the leaves\nleaf1.next = leaf2; leaf2.prev = leaf1;\nleaf2.next = leaf3; leaf3.prev = leaf2;\nleaf3.next = leaf4; leaf4.prev = leaf3;\n\n// Simulate finding the start leaf (in a real B+ tree this is O(log n))\nfunction findStartLeaf(rootLeaf, targetKey) {\n  let leaf = rootLeaf;\n  while (leaf && leaf.keys[leaf.keys.length - 1] < targetKey)\n    leaf = leaf.next;\n  return leaf;\n}\n\n// Range query: find all records where 12 <= key <= 42\nfunction rangeQuery(startLeaf, lo, hi) {\n  const results = [];\n  let   leaf    = findStartLeaf(startLeaf, lo);\n\n  // Walk the linked list of leaves\n  while (leaf !== null) {\n    for (let i = 0; i < leaf.keys.length; i++) {\n      const k = leaf.keys[i];\n      if (k > hi) return results; // past the upper bound — stop\n      if (k >= lo) results.push({ key: k, record: leaf.records[i] });\n    }\n    leaf = leaf.next; // O(1) hop to the next leaf\n  }\n  return results;\n}\n\nconst hits = rangeQuery(leaf1, 12, 42);\nhits.forEach(r => console.log(`key=${r.key} record=${r.record}`));\n// key=15 record=carol\n// key=20 record=dave\n// key=25 record=eve\n// key=30 record=frank\n// key=35 record=grace\n// key=40 record=heidi\n\n// Key insight: once we find the start leaf (O(log n) in real tree),\n// the rest of the scan is O(k) where k = number of results.\n// No backtracking through internal nodes is needed.",
      "explanation": "The `findStartLeaf` function simulates the O(log n) traversal from root to the first qualifying leaf. The `rangeQuery` function then walks the linked list in O(k) time, collecting results until it passes the upper bound. In a real B+ tree with millions of records and disk pages, this saves thousands of disk reads compared to a B-tree approach where each of the 6 results would require a separate O(log n) traversal."
    },
    {
      "type": "explanation",
      "title": "Common Misconceptions and Edge Cases",
      "content": "**Misconception 1: 'Balanced' means all subtrees are the same height.** AVL trees allow a height difference of 1 at every node — not zero. Red-Black trees allow the longest path to be twice the shortest. 'Balanced' in the context of search trees means 'height is O(log n)', not 'perfectly symmetric'. A perfectly balanced tree would be too expensive to maintain.\n\n**Misconception 2: You must implement AVL/RB from scratch in interviews.** Almost never. What interviewers actually care about is: (a) can you recognise when a problem needs a sorted dynamic structure, (b) do you know the O(log n) complexity guarantees, and (c) can you describe why plain BST is insufficient. Implementing rotations from scratch is typically reserved for systems programming or OS courses. Knowing the interface and properties is sufficient for 95% of software engineering interviews.\n\n**Misconception 3: B-tree equals B+ tree.** They are related but different. A plain B-tree stores data in both internal and leaf nodes. A B+ tree stores data ONLY in leaves, with internal nodes acting as a pure routing layer. Almost all production databases use B+ trees specifically for the linked-leaf range query advantage.\n\n**Edge case: Duplicate keys.** Neither AVL nor Red-Black trees as traditionally defined handle duplicate keys. Common solutions: store a count at each node, store a list at each node, or define that duplicates go left (or right) consistently. B-trees for databases use composite keys (data + primary key) to make all keys unique.\n\n**Edge case: Empty tree operations.** Both search and rotation code must handle null/empty nodes explicitly. A common bug is forgetting that a 'null node has height 0' when computing balance factors — the height of a null child must return 0 (or −1, depending on convention), not cause a null dereference.\n\n**Edge case: Single-node AVL tree.** A single node has balance factor 0 and is perfectly balanced by definition. The height is 1. No rotation is ever needed until a second node is inserted."
    },
    {
      "type": "explanation",
      "title": "Real-World Relevance: Where Balanced Trees Appear in Production",
      "content": "Balanced search trees are not textbook curiosities — they are the backbone of production systems you interact with every day.\n\n**Operating system schedulers:** The Linux kernel's Completely Fair Scheduler (CFS) uses a Red-Black tree to track all runnable processes. The leftmost node (smallest virtual runtime) is the next process to run. Insert and delete happen on every context switch — thousands of times per second — so O(log n) and bounded rotations are critical.\n\n**Standard library sorted maps:** Java's `TreeMap` and `TreeSet` (Red-Black), C++'s `std::map` and `std::set` (Red-Black), and Rust's `BTreeMap` (a B-tree variant) all implement ordered maps with O(log n) operations. These are the go-to structures when you need sorted iteration plus efficient point lookup.\n\n**Database indexes:** PostgreSQL, MySQL InnoDB, SQLite, Oracle, and SQL Server all use B+ trees as their default index structure. Every `CREATE INDEX` you execute creates a B+ tree on disk. The WHERE clause of a SELECT query navigates this B+ tree to avoid full table scans.\n\n**File systems:** NTFS (Windows) uses B+ trees to store file metadata. HFS+ and APFS (macOS) use B-trees. The ext4 filesystem uses an extent tree (a B-tree variant) for managing file block extents.\n\n**In-memory key-value stores:** Redis sorted sets are implemented as a skip list (a probabilistic alternative to balanced BSTs) plus a hash table. Redis explicitly chose skip lists over Red-Black trees for simpler implementation and better concurrent access patterns.\n\n**Browser CSS engines:** The Gecko layout engine (Firefox) uses AVL trees for certain layout data structures where read performance is paramount.\n\nUnderstanding balanced trees unlocks your ability to reason about the performance of all these systems — not just how to use them, but why they perform the way they do under load."
    },
    {
      "type": "callout",
      "variant": "info",
      "content": "**A brief history:** AVL trees (1962) predate the microprocessor. Adelson-Velsky and Landis published their paper in Russian; it was years before Western computer scientists fully absorbed it. Red-Black trees emerged from Bayer's 1972 symmetric binary B-trees and were given their colour terminology by Guibas and Sedgewick in 1978. B-trees were invented by Bayer and McCreight at Boeing Research Labs in 1970, specifically for disk-based storage — the paper title was 'Organization and Maintenance of Large Ordered Indices.' The fact that structures designed in 1962 and 1970 are still the default index structures in PostgreSQL and Linux in 2024 speaks to how fundamentally correct the underlying ideas are."
    },
    {
      "type": "callout",
      "variant": "tip",
      "content": "**Interview and system design guide:** (1) If asked 'what data structure for a sorted dynamic set with O(log n) operations?' — say balanced BST, specifically Red-Black or AVL. (2) If the interviewer asks which one — say Red-Black for write-heavy (standard library default), AVL for read-heavy. (3) If the system design question involves a database index or file system — always say B+ tree and explain the linked-leaf layer for range scans. (4) In JavaScript/Python, reach for a sorted array with binary search for small n (simpler); for large n with frequent modifications, use a library-provided sorted map or describe the B+ tree you would use. (5) The most common rotation mistake: forgetting to update heights AFTER reassigning children — update the lower node first, then the new root, or your heights will be wrong."
    },
    {
      "type": "key_takeaways",
      "items": [
        "A plain BST degenerates to O(n) height on sorted input — a common real-world case. Balanced search trees enforce O(log n) height by self-adjusting via rotations or node splits after every modification.",
        "AVL trees maintain a strict balance factor (|left_h − right_h| ≤ 1) at every node, guaranteeing height ≤ 1.44 log₂ n. Four rotation cases (LL, RR, LR, RL) restore balance after each insert; inserts need at most 2 rotations but deletes may need O(log n).",
        "Red-Black trees use five colour invariants — equal black-height on every root-to-null path, no consecutive red nodes — bounding height at 2 log₂(n+1). Inserts need at most 2 rotations; deletes need at most 3. This makes Red-Black the choice for write-heavy workloads and all major standard library sorted maps.",
        "B-trees generalise BSTs to nodes with up to M children. A single node fits one disk page; with M ≈ 200, a 4-level B-tree indexes billions of records with only 4 disk reads. All leaves remain at the same depth, maintained by splitting full nodes on insert and merging under-full nodes on delete.",
        "B+ trees store all records exclusively in leaf nodes, with internal nodes acting as a pure routing layer holding more keys. Leaves are connected as a doubly-linked list, enabling O(log n + k) range queries — the reason every major relational database uses B+ trees as its default index structure.",
        "The key practical tradeoff is AVL (shorter tree, faster reads) vs Red-Black (bounded write rotations, standard library default) vs B+ tree (disk-oriented, database indexes). The Big-O class is the same for all; the difference is in constant factors, rotation counts, and I/O patterns.",
        "Balanced trees are deployed at scale in Linux CFS (Red-Black), Java TreeMap / C++ std::map (Red-Black), PostgreSQL/MySQL indexes (B+ tree), NTFS/APFS file systems (B-tree), and browser layout engines (AVL). Understanding the invariants explains their performance characteristics in production."
      ]
    }
  ],
  "visualizer": {
    "type": "none",
    "caption": ""
  },
  "quiz": [
    {
      "id": "q1",
      "question": "Inserting the keys 1, 2, 3, 4, 5 in order into a plain BST produces a tree of what height?",
      "type": "mcq",
      "options": ["3 (balanced tree height)", "4 (near-balanced)", "5 (degenerate chain — height equals n)", "2 (perfectly balanced)"],
      "correct": 2,
      "explanation": "Sorted insertion always sends each new key to the rightmost position, creating a right-skewed chain. For n=5 keys, the chain has height 5. Every operation on this degenerate tree costs O(n), not O(log n)."
    },
    {
      "id": "q2",
      "question": "What is the balance factor of an AVL node, and what values are permitted?",
      "type": "mcq",
      "options": [
        "The ratio of left to right subtree sizes; must equal 1.0",
        "The height of the left subtree minus the height of the right subtree; must be -1, 0, or +1",
        "The number of nodes in the left subtree minus the right; must be within 10% of each other",
        "The colour of the node (red or black); must alternate with parent"
      ],
      "correct": 1,
      "explanation": "The AVL balance factor at a node is height(left) - height(right). A valid AVL tree requires this to be -1, 0, or +1 at every node. A value of ±2 indicates imbalance and triggers a rotation."
    },
    {
      "id": "q3",
      "question": "Which rotation sequence fixes the Left-Right (LR) imbalance case in an AVL tree?",
      "type": "mcq",
      "options": [
        "A single right rotation on the imbalanced node",
        "A single left rotation on the imbalanced node",
        "A left rotation on the left child, then a right rotation on the imbalanced node",
        "A right rotation on the right child, then a left rotation on the imbalanced node"
      ],
      "correct": 2,
      "explanation": "The LR case: the imbalanced node z is left-heavy, but its left child y is right-heavy. A single right rotation on z would not fix it. Instead: first rotate left on y (converting LR to LL), then rotate right on z. This is always exactly two rotations for the LR case."
    },
    {
      "id": "q4",
      "question": "What is the maximum height of a Red-Black tree with n nodes?",
      "type": "mcq",
      "options": ["log₂(n)", "1.44 × log₂(n)", "2 × log₂(n+1)", "n/2"],
      "correct": 2,
      "explanation": "The Red-Black invariants (equal black-height on all root-to-null paths; no two consecutive red nodes) mean the longest path alternates red and black, so its length is at most 2 × the shortest path. The shortest path length is the black-height bh ≥ ½ × height, giving height ≤ 2 × log₂(n+1). AVL's bound is tighter at 1.44 × log₂(n)."
    },
    {
      "id": "q5",
      "question": "Why are Red-Black trees preferred over AVL trees in most standard library sorted maps (Java TreeMap, C++ std::map)?",
      "type": "mcq",
      "options": [
        "Red-Black trees use less memory per node",
        "Red-Black trees produce a shorter tree, so searches are faster",
        "Red-Black trees require at most 3 rotations per delete vs AVL's O(log n), making writes faster",
        "Red-Black trees do not require rebalancing after insertion"
      ],
      "correct": 2,
      "explanation": "AVL's stricter balance means deletion may propagate rotations all the way to the root — O(log n) rotations. Red-Black trees cap deletes at 3 rotations (plus O(log n) cheap recolourings). For general-purpose sorted maps where inserts and deletes are common, this constant bound on rotations makes Red-Black consistently faster on writes."
    },
    {
      "id": "q6",
      "question": "In a B-tree of order M, what happens when a node overflows during an insert?",
      "type": "mcq",
      "options": [
        "The tree rebalances via rotations like an AVL tree",
        "The node is split: its median key is pushed up to the parent, and a new sibling node is created",
        "The excess key is discarded and re-inserted into the root",
        "The tree height doubles to accommodate the new key"
      ],
      "correct": 1,
      "explanation": "When a B-tree node accumulates more than M-1 keys, it splits: the median key is promoted to the parent, and the node's keys are divided between the original node and a new sibling. This may cascade to the parent if it also overflows. The only way a B-tree grows taller is if the root itself splits."
    },
    {
      "id": "q7",
      "question": "A B-tree with branching factor M=200 has height 4. Approximately how many records can it index?",
      "type": "mcq",
      "options": ["800 (200 × 4)", "40,000 (200²)", "8,000,000 (200³)", "1.6 billion (200⁴)"],
      "correct": 3,
      "explanation": "A B-tree of height 4 (4 levels) has up to M^4 leaves in the bottom level. With M=200, that is 200⁴ = 1,600,000,000 — 1.6 billion. This is why databases with billions of records still achieve lookups in 4-5 disk reads."
    },
    {
      "id": "q8",
      "question": "What structural feature of B+ trees makes range queries (e.g., 'find all rows where age BETWEEN 25 AND 40') much faster than in plain B-trees?",
      "type": "mcq",
      "options": [
        "B+ trees store all records in internal nodes for faster access",
        "B+ tree leaves are connected as a doubly-linked list, so after finding the first matching key with O(log n) traversal, the rest of the range is a linear leaf scan",
        "B+ trees use hash buckets at each leaf for O(1) lookup within a page",
        "B+ trees duplicate keys across multiple leaves to allow parallel range scans"
      ],
      "correct": 1,
      "explanation": "In a B+ tree, all records live in leaf nodes and leaves are linked sequentially. A range query navigates from the root to the first qualifying leaf in O(log n) disk reads, then walks the linked list to collect all results in O(k/L) additional reads where k is the result count and L is the leaf capacity. In a plain B-tree, data in internal nodes breaks this linear scan pattern."
    },
    {
      "id": "q9",
      "question": "Which of these systems uses a Red-Black tree in its implementation?",
      "type": "mcq",
      "options": [
        "PostgreSQL's default index (B+ tree on disk)",
        "The Linux Completely Fair Scheduler (CFS) for tracking runnable processes",
        "JavaScript's built-in Map object (hash table)",
        "MySQL InnoDB's primary key index (clustered B+ tree)"
      ],
      "correct": 1,
      "explanation": "The Linux CFS stores all runnable processes in a Red-Black tree keyed by virtual runtime. The leftmost node (smallest vruntime) is the next process to schedule. Insert and delete happen on every context switch, so bounded rotations and O(log n) per operation are essential. PostgreSQL and MySQL use B+ trees; JS Map is a hash table."
    },
    {
      "id": "q10",
      "question": "After inserting a new red node into a Red-Black tree, the node's parent is also red (violating invariant 4). The uncle node is RED. What is the correct fix?",
      "type": "mcq",
      "options": [
        "Perform a rotation at the grandparent to restore balance immediately",
        "Recolour the parent and uncle to black and the grandparent to red, then check the grandparent's parent for violations",
        "Recolour only the new node to black to eliminate the consecutive reds",
        "Split the grandparent node as in a B-tree insertion"
      ],
      "correct": 1,
      "explanation": "When the uncle is RED, no rotation is needed yet. Recolour parent and uncle to black, and grandparent to red. This fixes the local violation but may push a red-red conflict up to the grandparent. The algorithm then checks the grandparent's parent, propagating up to O(log n) recolourings. At most 2 rotations occur during the entire insert, only when an uncle is BLACK."
    }
  ]
}
